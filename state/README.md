# Module State

Each `Mxx.json` is the coordination record for one module.

Agents must not infer ownership from conversation memory.

The JSON state and filesystem lock are the coordination source.

Never manually delete a lock belonging to another agent.
