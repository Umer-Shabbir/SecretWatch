# Security Rules

- Never store raw secrets.
- Never log raw GitHub tokens.
- Never include secrets in test fixtures.
- Redact sensitive values before persistence.
- Enforce authorization server-side.
- Treat flagging as an auditable action.
- Keep admin review enabled by default unless explicitly configured otherwise.
